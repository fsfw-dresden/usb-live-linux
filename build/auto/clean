#!/bin/sh

set -e

# Move old cache to new build folder
[ ! -d cache ] || mv -vf cache build/

# this cleans chroot, binary, stage & source
# c.f. /usr/lib/live/build/clean
lb clean noauto "${@}"

# remove previous live-build settings
rm -rf config/
