os-prober       # utility to detect other OSes on a set of drives
rsync           # fast, versatile, remote (and local) file-copying tool
util-linux      # (for lsblk in initramdisk)
strace          # (strace in initramdisk)
