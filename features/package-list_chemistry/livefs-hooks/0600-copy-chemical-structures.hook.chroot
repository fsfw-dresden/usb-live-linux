#!/bin/bash

CHEM_DIR="/usr/share/chemical-structures"
TARGET_DIR="/usr/share/sample-media-files"

mkdir -pv "${TARGET_DIR}"
cp -av "${CHEM_DIR}" "${TARGET_DIR}/"
find "${TARGET_DIR}/${CHEM_DIR##*/}" -name '*.html' -delete
